import { DisableElementPipe } from './disable-element.pipe';

describe('DisableElementPipe', () => {
  it('create an instance', () => {
    const pipe = new DisableElementPipe();
    expect(pipe).toBeTruthy();
  });
});
